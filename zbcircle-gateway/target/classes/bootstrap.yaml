spring:
  application:
    name: zbcircle-gateway

  main:
    allow-bean-definition-overriding: true

  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: 94a4c5b7-425e-444d-a025-7fcfb3cf0f98
        group: zbcircle
      config:
        namespace: 94a4c5b7-425e-444d-a025-7fcfb3cf0f98
        group: zbcircle-gateway
        file-extension: yaml
        refresh-enabled: true



#    gateway:
#      #      filter:
#      #        strip-prefix:
#      #          enabled: true
##      discovery:
##        locator:
##          enabled: true #开启从注册中心动态创建路由的功能，利用微服务名进行路由
#      routes: # 网关路由配置
#        - id: zbcircle-auth # 路由id，自定义，只要唯一即可
#          uri: lb://zbcircle-auth # 路由的目标地址 lb就是负载均衡，后面跟服务名称
#          predicates: # 路由断言，也就是判断请求是否符合路由规则的条件
#           - Path=/auth/** # 这个是按照路径匹配，只要以/content/开头就符合要求
#  #          filters:
#  #            - RewritePath=/api/?(?<segment>.*), /$\{segment}

